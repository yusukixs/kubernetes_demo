# kubernetes_demo

## Initialize on Local

```
$ git clone https://github.com/yusukixs/kubernetes_demo.git
$ cd kubernetes_demo
$ npm install
$ node server.js
```

Open `http://localhost:3000` on the browser.